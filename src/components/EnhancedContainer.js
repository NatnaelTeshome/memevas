import {styled} from "@mui/system"

const EnhancedContainer = styled("div") ({
    width : "100vw",
    height : "90vh",
    backgroundColor : "#E4EC8A",
    display : "flex",
    flexDirection : "column",
    alignItems : "center",
})

export default EnhancedContainer